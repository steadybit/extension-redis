apiVersion: v1
kind: Service
metadata:
  name: {{ include "steadybit-extension-redis.fullname" . }}
  labels:
    {{- include "steadybit-extension-redis.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "steadybit-extension-redis.selectorLabels" . | nindent 4 }}
